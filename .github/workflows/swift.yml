name: CI

on:
  pull_request:
    branches: 
      - development
  workflow_dispatch:

env:
  # Use latest Xcode available on macOS-14 (Xcode 16.2 as of 2025)
  DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer

jobs:
  # MARK: - Swift Package Validation
  validate:
    name: Validate Package
    runs-on: macos-14  # Use specific macOS version for consistency
    steps:
    - uses: actions/checkout@v4
    
    # Set up Swift/Xcode version explicitly
    - name: Setup Swift
      uses: swift-actions/setup-swift@v2
      with:
        swift-version: "6.1.0"  # Match your Package.swift version
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    
    - name: Cache Swift Package Manager
      uses: actions/cache@v4
      with:
        path: .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    
    - name: Validate Package
      run: swift package dump-package

  # MARK: - Build & Test
  test:
    name: Test on ${{ matrix.platform }}
    runs-on: macos-14  # Use specific macOS version for consistency
    needs: validate
    strategy:
      matrix:
        platform: [macOS]  # Only test macOS for now due to SPM limitations
        
    steps:
    - uses: actions/checkout@v4
    
    # Set up Swift/Xcode version explicitly
    - name: Setup Swift
      uses: swift-actions/setup-swift@v2
      with:
        swift-version: "6.1.0"  # Match your Package.swift version
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    
    - name: Cache Swift Package Manager
      uses: actions/cache@v4
      with:
        path: .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    
    - name: Resolve dependencies
      run: swift package resolve
    
    - name: Build for ${{ matrix.platform }}
      run: swift build
    
    - name: Run tests
      run: swift test --enable-code-coverage
    
    - name: Generate code coverage
      run: |
        xcrun llvm-cov export -format="lcov" \
          .build/debug/CoinGeckoSwiftSDKPackageTests.xctest/Contents/MacOS/CoinGeckoSwiftSDKPackageTests \
          -instr-profile .build/debug/codecov/default.profdata > coverage.lcov
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: coverage.lcov
        flags: swift
        name: codecov-swift

  # MARK: - Platform Compatibility Check (using Xcode)
  platform-check:
    name: Platform Compatibility Check
    runs-on: macos-14
    needs: validate
    strategy:
      matrix:
        platform: [iOS, tvOS, watchOS]
        
    steps:
    - uses: actions/checkout@v4
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    
    - name: Build for ${{ matrix.platform }}
      run: |
        case "${{ matrix.platform }}" in
          iOS)
            # Use xcodebuild for iOS instead of swift build
            xcodebuild -scheme CoinGeckoSwiftSDK -destination 'generic/platform=iOS Simulator' -sdk iphonesimulator build
            ;;
          tvOS)
            # Use xcodebuild for tvOS instead of swift build
            xcodebuild -scheme CoinGeckoSwiftSDK -destination 'generic/platform=tvOS Simulator' -sdk appletvsimulator build
            ;;
          watchOS)
            # Use xcodebuild for watchOS instead of swift build
            xcodebuild -scheme CoinGeckoSwiftSDK -destination 'generic/platform=watchOS Simulator' -sdk watchsimulator build
            ;;
        esac

  # MARK: - Fastlane CI
  fastlane-ci:
    name: Fastlane CI
    runs-on: macos-14  # Use specific macOS version for consistency
    needs: validate
    
    steps:
    - uses: actions/checkout@v4
    
    # Set up Swift/Xcode version explicitly
    - name: Setup Swift
      uses: swift-actions/setup-swift@v2
      with:
        swift-version: "6.1.0"  # Match your Package.swift version
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    
    # Ruby setup with Bundler
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2.2'
        bundler-cache: true # runs 'bundle install' and caches gems
    
    - name: Cache Swift Package Manager
      uses: actions/cache@v4
      with:
        path: .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    
    - name: Install Ruby dependencies
      run: bundle install
    
    - name: Run Fastlane CI
      run: bundle exec fastlane ci
    
    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: Build/TestResults/

  # MARK: - Danger PR Review
  danger:
    name: Danger PR Review
    runs-on: macos-14  # Use specific macOS version for consistency
    if: github.event_name == 'pull_request'
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Danger needs full git history
    
    # Ruby setup with Bundler
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2.2'
        bundler-cache: true
    
    - name: Install SwiftLint
      run: brew install swiftlint
    
    - name: Install Ruby dependencies
      run: bundle install
    
    - name: Run Danger
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: bundle exec danger

  # MARK: - SwiftLint
  lint:
    name: SwiftLint
    runs-on: macos-14  # Use specific macOS version for consistency

    steps:
      - uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint --strict

  # MARK: - Release
  release:
    name: Create Release
    runs-on: macos-14  # Use specific macOS version for consistency
    needs: [test, platform-check, fastlane-ci, lint, danger]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    # Set up Swift/Xcode version explicitly
    - name: Setup Swift
      uses: swift-actions/setup-swift@v2
      with:
        swift-version: "6.1.0"  # Match your Package.swift version
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    
    # Ruby setup for release tasks
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2.2'
        bundler-cache: true
    
    - name: Install Ruby dependencies
      run: bundle install
    
    - name: Build release
      run: swift build -c release
    
    - name: Create tag
      run: |
        VERSION=$(date +'%Y.%m.%d')
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        git tag $VERSION
        git push origin $VERSION
    
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.VERSION }}
        release_name: Release ${{ env.VERSION }}
        draft: false
        prerelease: false